# Sources Bibliography

1. **DuckDuckGo Search:** Used `duckduckgo-search` package because it requires no API key and provides reliable enough snippets for the "Dirty" agent's context.
1. **FastAPI & HTMX:** Chosen based on Composite Instructions for web apps to avoid complex JS SPAs while still providing dynamic updates.
